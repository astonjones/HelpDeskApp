@model IEnumerable<HelpDeskApp.Models.Ticket>;

<div class="d-flex justify-content-around">
    <div>
        <h5>Got an IT problem? Create a ticket</h5>
    </div>
    <button class="btn btn-primary">Create Ticket</button>
</div>

<div>
    <h3>These are your active tickets</h3>
    <div>

        @if (Model.Count() > 0)
        {
            <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Category
                    </th>
                    <th>
                        Description
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="30%">@obj</td>
                        <td width="30%">@obj.PhoneNumber</td>
                        <td width="50%">@obj.Category</td>
                        <td class="w-75 btn-group" role="group">
                            <a asp-controller="Category" asp-route-Id="@obj.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                Edit
                            </a>
                            <a asp-controller="Category" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
            </table>
        }
        else
        {
            <p>You have not submitted any tickets</p>
        }
    </div>

</div>